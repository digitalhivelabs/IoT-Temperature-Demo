info:
  name: Get Summary
  type: http
  seq: 4

http:
  method: GET
  url: "{{ApiUrl}}/api/telemetry/summary"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
