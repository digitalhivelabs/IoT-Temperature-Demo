info:
  name: Get Current Config
  type: http
  seq: 3

http:
  method: GET
  url: "{{ApiUrl}}/api/device/temperature-pharma-001/status"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
