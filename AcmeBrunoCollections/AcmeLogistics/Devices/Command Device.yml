info:
  name: Command Device
  type: http
  seq: 1

http:
  method: POST
  url: "{{ApiUrl}}/api/device/temperature-pharma-001/command"
  body:
    type: json
    data: |-
      {
        "command": "activate_buzzer",
        "correlationId": "shipment-tracker-001"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
